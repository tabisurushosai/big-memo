{
  "name": "big-memo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.portable.json && tsc --noEmit && vite build",
    "typecheck": "tsc -p tsconfig.portable.json && tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.42",
    "typescript": "^6.0.3",
    "vite": "^8.0.14"
  }
}
